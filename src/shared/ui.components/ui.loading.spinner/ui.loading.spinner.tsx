import React from 'react';

type Props = {}

const UiLoadingSpinner: React.FC<Props> = (props) => {

    return (
        <div className="ui-loading-spinner">
            ui loading spinner here
        </div>
    );
}

export default UiLoadingSpinner;
